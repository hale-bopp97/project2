const User = require('./User');
const Project = require('./Project');
const Quiz = require('./Quiz');

User.hasMany(Project, {
  foreignKey: 'user_id',
  onDelete: 'CASCADE'
});

Project.belongsTo(User, {
  foreignKey: 'user_id'
});

module.exports = { User, Project, Quiz };
